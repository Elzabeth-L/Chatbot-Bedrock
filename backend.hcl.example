bucket       = "REPLACE_WITH_EXISTING_STATE_BUCKET"
key          = "bedrock-rag-demo/demo/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true

# Legacy only: DynamoDB state locking is deprecated. Uncomment only when required.
# dynamodb_table = "REPLACE_WITH_LEGACY_LOCK_TABLE"
